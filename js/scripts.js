$(document).ready(function(){
  $("form").submit(function(event){
    event.preventDefault()

    var sentence = ($("#input").val()).split(" ");
    console.log(sentence);
    var newArray = sentence.forEach(function(word){
      if word > characters (3).push(newArray){  
      }else{

      }
    };




  });
});
